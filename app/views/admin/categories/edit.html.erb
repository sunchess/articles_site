<div>
  <%= form_for @category, :url => admin_category_path(@category) do |f| %>
    <%= render "shared/error_messages", :target => @category %>
    <p>
      <%= f.label :name %>  <%= f.text_field :name %> <%= f.label :position %> <%= f.text_field :position, :size => 3 %>
    </p>
    <%= f.submit %>
  <% end %>
</div>
