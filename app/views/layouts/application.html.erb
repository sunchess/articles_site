<!DOCTYPE html>
<html>
<head>
  <title><% if render_breadcrumbs(:full=>true).present?%>
    <%= "#{render_breadcrumbs(:no_html=>true, :separator => ' / ', :full=>true).html_safe} :: " %>
  <% end %>Jsovet - женские советы</title>
	<link href="/stylesheets/style.css" rel="stylesheet" type="text/css" />
	<!--[if IE]><style>.content{behavior: url(/stylesheets/PIE.htc); position: relative;}</style><![endif]-->
  <%= javascript_include_tag :defaults %>
  <%= yield( :editor ) %>
  <%= csrf_meta_tag %>
</head>
<body>
	<div class="header">
		<p><a href="/"><span></span>Jsovet - женские советы</a></p>
                <%= render :partial => "layouts/users_login_register" %>
	</div><!-- END header -->
	<div class="wrapper">
		<div class="sidebar">
			<div class="sidebar_block">
				<div class="block_top"></div>
					<div class="block_content">
                                          <% if @category %>
                                            <h2><%= @category.name %></h2>
                                          <% else %>
                                            <h2>Категории</h2>
                                          <% end %>
						<ul>
                                                  <%= render :partial => "layouts/category", :collection => Category.all %>
						</ul>
					</div><!-- END block_content -->
				<div class="block_bottom"></div>
			</div><!-- END sidebar_block -->
		</div><!-- END sidebar -->
		<div class="content">

                    <%= render :partial => "layouts/search_form" %>
			<div class="breadcrumbs">
                              <%= render_breadcrumbs %>
			</div><!-- END breadcrumbs -->

			<div class="post">
                          <% if flash[:notice] %>
                            <div class="notice">
                              <%= notice %>
                            </div>
                          <% end %>
                          <% if flash[:alert] %>
                            <div class="alert">
                              <%= flash[:alert] %>
                            </div>
                          <% end %>
                          <%= yield %>
		</div><!-- END content -->
	</div><!-- END wrapper -->
	<div class="footer">		
		<ul>
			<li><a href="#">Авторы</a></li>
			<li><a href="#">О проекте</a></li>
			<li><a href="#">Контакты</a></li>
		</ul>
                <p>&copy; 2010-<%= Time.now.year %> Jsovet. Все права защищены.<br/>
                  Полная или частичная перепечатка материалов допускается только с разрешения редакции.</p>
		<div class="shoe"></div>
	</div><!-- END footer -->
</body>
</html>
